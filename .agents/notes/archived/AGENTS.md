# AGENTS.md — Archived Agent Notes

Files under this tree are frozen historical snapshots. Never edit, translate, reformat, update, move, or delete them, and do not treat them as authority for current behavior. Active prose may still link into an archived note when it intentionally cites history.

An archival change moves the complete English/Chinese/sidecar triplet, retains `Status: implemented`, inserts the same `Archived: YYYY-MM-DD` line immediately below that status, and re-records the sidecar.
