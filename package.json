{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "DSH Desktop — virtual-webserver interceptor spike",
  "license": "MIT",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "start": "pnpm --filter dsh-plugin-desktop-connection build && pnpm --filter dsh-plugin-desktop start",
    "test": "pnpm --filter dsh-plugin-desktop test"
  }
}
